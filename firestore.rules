rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /products/{productId} {
      allow read: if true;
      allow write: if false; // Temporarily disable writes to isolate read issue
    }
    // All other collections are implicitly denied for this diagnostic step.
  }
}
