{
  "entities": {
    "Asset": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Asset",
      "type": "object",
      "description": "Represents a physical asset such as equipment, structure, or component that needs inspection.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Asset." },
        "name": { "type": "string", "description": "The name of the asset." },
        "description": { "type": "string", "description": "A detailed description of the asset." },
        "location": { "type": "string", "description": "The physical location of the asset." },
        "qrCode": { "type": "string", "description": "The QR code associated with the asset for easy identification." },
        "thumbnailUrl": { "type": "string", "description": "URL for the asset's thumbnail image." }
      },
      "required": ["id", "name"]
    },
    "Inspection": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Inspection",
      "type": "object",
      "description": "Represents an inspection event performed on an asset.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Inspection." },
        "jobId": { "type": "string", "description": "Reference to the Job this inspection is part of." },
        "assetId": { "type": "string", "description": "Reference to the Asset being inspected." },
        "inspectorId": { "type": "string", "description": "Reference to the User (Inspector) performing the inspection." },
        "scheduleDate": { "type": "string", "format": "date-time", "description": "The date the inspection is scheduled." },
        "completionDate": { "type": "string", "format": "date-time", "description": "The date the inspection was completed." },
        "status": { "type": "string", "description": "The status of the inspection." },
        "reportId": { "type": "string", "description": "Reference to the generated InspectionReport." }
      },
      "required": ["id", "jobId", "assetId", "inspectorId", "scheduleDate", "status"]
    },
    "Report": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Report",
      "type": "object",
      "description": "Represents the report generated after an inspection.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Report." },
        "inspectionId": { "type": "string", "description": "Reference to the Inspection this report is for." },
        "reportData": { "type": "string", "description": "The actual report data, likely stored as a JSON string." },
        "auditorId": { "type": "string", "description": "Reference to the User (Auditor) who approved the report." }
      },
      "required": ["id", "inspectionId", "reportData"]
    },
    "Job": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Job",
      "type": "object",
      "description": "Represents a job request created by a client for NDT inspection services.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Job." },
        "clientId": { "type": "string", "description": "Reference to the User (Client) who created the job." },
        "providerId": { "type": "string", "description": "Reference to the User (Provider) awarded the job." },
        "assetIds": {
          "type": "array",
          "description": "An array of Asset IDs this job pertains to. This is mandatory for client-created jobs.",
          "items": { "type": "string" }
        },
        "location": { "type": "string", "description": "The location where the job needs to be performed." },
        "technique": { "type": "string", "description": "The NDT technique required for the job." },
        "status": { "type": "string", "description": "Current status of the job (e.g., Posted, Assigned, Completed)." }
      },
      "required": ["id", "clientId", "location", "technique", "status"]
    },
    "Bid": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Bid",
      "type": "object",
      "description": "Represents a bid submitted by an inspector for a job.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Bid." },
        "jobId": { "type": "string", "description": "Reference to the Job." },
        "inspectorId": { "type": "string", "description": "Reference to the User (Inspector) who submitted the bid." },
        "price": { "type": "number", "description": "The price quoted in the bid." }
      },
      "required": ["id", "jobId", "inspectorId", "price"]
    },
    "User": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "User",
      "type": "object",
      "description": "Represents a user of the application.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the User." },
        "name": { "type": "string", "description": "The name of the user." },
        "email": { "type": "string", "format": "email", "description": "The email address of the user." },
        "role": { "type": "string", "enum": ["Client", "Inspector", "Auditor", "Admin"], "description": "The role of the user." },
        "companyId": { "type": "string", "description": "The ID of the company this user belongs to." }
      },
      "required": ["id", "name", "email", "role", "companyId"]
    },
    "Equipment": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Equipment",
      "type": "object",
      "description": "Represents a piece of NDT equipment used by an inspector or service provider.",
      "properties": {
        "id": { "type": "string", "description": "Unique identifier for the Equipment." },
        "name": { "type": "string", "description": "The name of the equipment." },
        "type": { "type": "string", "description": "The category of equipment (e.g., Instrument, Probe)." },
        "providerId": { "type": "string", "description": "ID of the provider company that owns this equipment." },
        "status": { "type": "string", "description": "Current status (e.g., Available, In Use, Calibration Due)." },
        "nextCalibration": { "type": "string", "format": "date", "description": "Date of next required calibration." },
        "thumbnailUrl": { "type": "string", "description": "URL for the equipment's thumbnail image." }
      },
      "required": ["id", "name", "type", "providerId", "status"]
    },
     "Company": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Company",
      "type": "object",
      "description": "Represents a company entity, which can be a Client, Provider, or Auditor.",
      "properties": {
        "id": { "type": "string" },
        "name": { "type": "string" },
        "type": { "type": "string", "enum": ["Client", "Provider", "Auditor"] },
        "contactPerson": { "type": "string" },
        "contactEmail": { "type": "string", "format": "email" },
        "logoUrl": { "type": "string", "format": "uri" },
        "brandColor": { "type": "string" }
      },
      "required": ["id", "name", "type"]
    },
    "Subscription": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Subscription",
      "type": "object",
      "description": "Represents a company's subscription plan.",
      "properties": {
        "id": { "type": "string" },
        "companyId": { "type": "string" },
        "plan": { "type": "string" },
        "status": { "type": "string", "enum": ["Active", "Trialing", "Past Due", "Canceled", "Payment Failed"] },
        "startDate": { "type": "string", "format": "date" },
        "endDate": { "type": "string", "format": "date" },
        "userLimit": { "type": "number" },
        "dataLimitGB": { "type": "number" }
      },
      "required": ["id", "companyId", "plan", "status", "startDate"]
    },
    "Chat": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Chat",
      "type": "object",
      "description": "A chat thread, usually associated with a job.",
      "properties": {
        "id": { "type": "string" },
        "jobId": { "type": "string" },
        "participants": { "type": "array", "items": { "type": "string" } },
        "lastMessage": { "type": "string" }
      },
      "required": ["id", "participants"]
    },
    "Message": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Message",
      "type": "object",
      "description": "A single message within a chat thread.",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" },
        "senderId": { "type": "string" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "text", "senderId", "timestamp"]
    },
    "SupportChat": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SupportChat",
      "type": "object",
      "description": "A support chat thread between a user and the platform support team.",
      "properties": {
        "id": { "type": "string" },
        "userId": { "type": "string", "description": "ID of the user who initiated the chat." },
        "userName": { "type": "string" },
        "userCompany": { "type": "string" },
        "subject": { "type": "string" },
        "status": { "type": "string", "enum": ["Open", "Closed"] },
        "lastMessage": { "type": "string" },
        "lastMessageTimestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "userId", "status", "subject"]
    },
    "SupportMessage": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "SupportMessage",
      "type": "object",
      "description": "A single message within a support chat thread.",
      "properties": {
        "id": { "type": "string" },
        "text": { "type": "string" },
        "senderId": { "type": "string" },
        "senderName": { "type": "string" },
        "isAdmin": { "type": "boolean" },
        "timestamp": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "text", "senderId", "timestamp"]
    }
  },
  "auth": {
    "providers": ["password", "anonymous"]
  },
  "firestore": {
    "structure": [
      {
        "path": "/users/{userId}",
        "definition": { "entityName": "User", "schema": { "$ref": "#/entities/User" }, "description": "Stores user profiles." }
      },
      {
        "path": "/assets/{assetId}",
        "definition": { "entityName": "Asset", "schema": { "$ref": "#/entities/Asset" }, "description": "Stores all assets." }
      },
      {
        "path": "/jobs/{jobId}",
        "definition": { "entityName": "Job", "schema": { "$ref": "#/entities/Job" }, "description": "Stores all jobs from all clients." }
      },
      {
        "path": "/bids/{bidId}",
        "definition": { "entityName": "Bid", "schema": { "$ref": "#/entities/Bid" }, "description": "Stores all bids for all jobs." }
      },
      {
        "path": "/inspections/{inspectionId}",
        "definition": { "entityName": "Inspection", "schema": { "$ref": "#/entities/Inspection" }, "description": "Stores all inspections for all jobs." }
      },
      {
        "path": "/reports/{reportId}",
        "definition": { "entityName": "Report", "schema": { "$ref": "#/entities/Report" }, "description": "Stores all reports for all inspections." }
      },
      {
        "path": "/equipment/{equipmentId}",
        "definition": { "entityName": "Equipment", "schema": { "$ref": "#/entities/Equipment" }, "description": "Stores all equipment for all providers." }
      },
       {
        "path": "/companies/{companyId}",
        "definition": { "entityName": "Company", "schema": { "$ref": "#/entities/Company" }, "description": "Stores all company profiles (clients, providers, auditors)." }
      },
      {
        "path": "/subscriptions/{subscriptionId}",
        "definition": { "entityName": "Subscription", "schema": { "$ref": "#/entities/Subscription" }, "description": "Stores subscription details for all companies." }
      },
      {
        "path": "/chats/{chatId}",
        "definition": { "entityName": "Chat", "schema": { "$ref": "#/entities/Chat" }, "description": "Stores chat thread metadata." }
      },
      {
        "path": "/supportChats/{supportChatId}",
        "definition": { "entityName": "SupportChat", "schema": { "$ref": "#/entities/SupportChat" }, "description": "Stores support chat threads between users and the platform admin." }
      }
    ],
    "reasoning": "The Firestore structure is designed with a flattened data model. Each major entity (users, assets, jobs, companies, etc.) is a top-level collection. This approach simplifies querying (e.g., fetching all open jobs) and makes security rules more direct and manageable. Relationships between documents are maintained using stored IDs (e.g., a 'job' document contains a 'clientId'). This avoids deep nesting, which can complicate security and data retrieval, adhering to Firestore best practices for scalable and secure applications. Chat messages and Support Messages are stored in a subcollection under a chat document to allow for efficient loading of message history for a specific chat."
  }
}
